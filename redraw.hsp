//redraw p1
//p1に0を設定すると、描画が一時停止され、描画内容は仮想画面に描画される。
//p1に1を設定すると、描画が再開され、描画内容はメインの画面に描画される。

cls 					//画面を初期化
x=0 					//変数xに0を代入
y=0 					//変数yに0を代入

repeat 					//繰り返し開始
	redraw 0			//描画を一時停止
		cls				//画面内の物を全て消す
		pos x,y 		//座標指定
		mes "(^_^)ﾉｼ" 	//動かす文字設定
	redraw 1 			//ここまでをまとめて描画

	stick key 			//変数Keyにキーボード入力を代入

	if key=1:x-=1		//左
	if key=2:y-=1		//上
	if key=4:x+=1		//右
	if key=8:y+=1		//下

	await 1				//応答を待つ
loop					//繰り返し終了
